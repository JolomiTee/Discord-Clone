const PersonalInfo = () => {
	return (
		<div>
			This section will enable users to edit their profile name, image,
			password and usernames, maybe also a banner image, lets see how it goes
			first.
		</div>
	);
};

export default PersonalInfo;
